# ============================================================
# LOCAL DEVELOPMENT REQUIREMENTS
# ============================================================
# This file is a subset of requirements.txt, excluding packages
# that need a C compiler to build on Windows (e.g. aiokafka).
# Those packages run only inside Docker containers anyway.
#
# Usage:  pip install -r requirements-dev.txt
# ============================================================

# Web Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Database & ORM
sqlmodel==0.0.14
psycopg2-binary==2.9.9
asyncpg==0.29.0

# Caching (Redis — pure Python, works on Windows)
redis==5.0.1

# aiokafka is EXCLUDED here — it requires a C compiler.
# It runs only inside Docker. We mock it in local tests.

# External API & Scraping
httpx==0.26.0
beautifulsoup4==4.12.3
howlongtobeatpy==0.1.2

# Utilities
python-dotenv==1.0.0
pydantic-settings==2.1.0

# Testing & Quality
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-mock==3.12.0
freezegun==1.4.0
pylint==3.0.3
